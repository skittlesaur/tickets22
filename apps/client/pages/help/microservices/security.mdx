import HelpLayout from '@layouts/help'
import Endpoint from '@components/help/endpoint'
import { SECURITY_SERVICE_URL } from '@services/constants'

# Security Microservice

The security microservice is responsible for protecting against fraud and/or malicious attacks. It's also used for user
authentication and authorization.

## Public API

<Endpoint
  baseUrl={SECURITY_SERVICE_URL}
  title="Get your user profile"
  path="/me"
  method="POST"
  requiresToken={true}
  responses={[
  {
    status: 200,
    res:
`{
  "id": "clb2egklgqteo2x8dj",
  "name": "Baraa",
  "email": "hey.baraa@gmail.com"
}`
  },
  {
    status: 400,
    res:
`{
  "status": 400,
  "code": "Bad Request",
  "message": "Token was not supplied"
}`
  },
  {
    status: 401,
    res:
`{
  "status": 401,
  "code": "Unauthorized",
  "message": "Token verification failure, the token may have expired"
}`
  },
  {
    status: 401,
    res:
`{
  "status": 401,
  "code": "Unauthorized",
  "message": "Invalid user id in the token"
}`
  }
  ]}
/>

export default ({ children }) => (
  <HelpLayout
    title="Security Microservice"
  >
    {children}
  </HelpLayout>
)